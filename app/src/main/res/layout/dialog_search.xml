<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    android:layout_width="wrap_content"
    app:cardCornerRadius="20dp"
    android:backgroundTint="@color/white"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="wrap_content">
<RelativeLayout
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <SearchView
        android:id="@+id/admin_searchSV"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:backgroundTint="#DCDCDC"
        android:closeIcon="@drawable/ic_close_white_24dp"
        android:layout_marginHorizontal="40dp"
android:theme="@style/SearchViewStyle"
android:searchIcon="@drawable/search"        android:layout_marginTop="10dp"
        android:iconifiedByDefault="false"
        android:textColor="#320202"

        android:textSize="13dp">

        <requestFocus />
    </SearchView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/found_itemsRV"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@+id/admin_searchSV"
        android:layout_marginHorizontal="20dp"
        android:layout_marginVertical="20dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"

        app:layout_constraintVertical_bias="0.014" />

</RelativeLayout>
</androidx.cardview.widget.CardView>
